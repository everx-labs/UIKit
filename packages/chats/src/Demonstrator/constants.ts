// eslint-disable-next-line no-shadow
export enum VisibilityState {
    Closed = 0,
    Measurement = 1,
    Opened = 2,
}
// eslint-disable-next-line no-shadow
export enum AnimationProgress {
    Closed = 0,
    Opened = 1,
}
