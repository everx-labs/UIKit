// eslint-disable-next-line no-shadow
export enum DuplicateState {
    Initial = 0,
    Measurement = 1,
    Opened = 2,
    Closed = 3,
}
// eslint-disable-next-line no-shadow
export enum VisibilityState {
    Closed = 0,
    Opened = 1,
}
